---
- name: Load MicroK8s kernel modules
  ansible.builtin.systemd_service:
    name: systemd-modules-load.service
    state: restarted

- name: Apply Kubernetes sysctl settings
  ansible.builtin.command: sysctl --system
  changed_when: true
